
# Monitor:
## Description:
- Uses `psutil` to monitor hardware statistics.
- More accurate than `stats` but requires being ran within the container.

## Usage:
- `python3 main.py precision(ms) network_interface output.csv`
